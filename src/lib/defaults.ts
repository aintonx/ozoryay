/**
 * Исходные значения для сида базы.
 *
 * Приложение читает настройки ИЗ БАЗЫ и меняет их через админку.
 * Этот файл — единственный источник для `prisma/seed.ts` и запасной вариант
 * на то время, пока база ещё не подключена. В компонентах его импортировать
 * нельзя: они получают настройки пропсами с сервера.
 */
export const SEED_SETTINGS = {
  separationStart: "2026-06-30T16:33:00+03:00",
  herTimezone: "Europe/Moscow",
  herCity: "Краснодар",
  myCity: "Петербург",
  /**
   * Где она — отсюда считается настоящее небо. Точность до города более чем
   * достаточна: сдвиг в несколько километров не меняет вид звёзд.
   */
  herLat: 45.075,
  herLon: 38.937,
  /** Петербург → Краснодар по большому кругу, 59.9386/30.3141 → 45.0355/38.9753. */
  distanceKm: 1754,
  /** Начальный азимут того же пути: юго-юго-восток. */
  bearingDeg: 157.0,
  dawnAt: null as string | null,
  daysFloor: 0,
};

export type Settings = typeof SEED_SETTINGS;

/**
 * Что сейчас включено на сайте.
 *
 * Прожектор с фотографиями сделан и работает — код цел, фотографии на месте.
 * Он просто выключен до времени: включить обратно значит поставить здесь
 * `true`, ничего больше делать не придётся.
 */
export const FEATURES = {
  projector: false,
} as const;
